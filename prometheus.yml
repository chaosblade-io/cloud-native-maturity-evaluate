global:
  scrape_interval: 30s
  evaluation_interval: 30s

scrape_configs:
  - job_name: sesora
    metrics_path: /api/analyze/export/prometheus
    static_configs:
      - targets:
          - api:8000
    honor_labels: true
